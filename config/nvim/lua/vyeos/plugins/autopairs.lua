return {
  'windwp/nvim-autopairs',
  event = "InsertEnter",
  config = true -- This uses the default config, which is great
  -- If you use cmp (autocomplete), you can add integration logic here later
}
